// "use client";

// import React, { forwardRef, type HTMLProps } from "react";

// import { cn } from "@/lib/utils";

// interface ProgressBarProps extends HTMLProps<HTMLDivElement> {
//   value: number;
// }

// const ProgressBar = forwardRef<HTMLDivElement, ProgressBarProps>(
//   ({ className, value }, _ref) => (
//     <div className="my-1 h-3 w-full overflow-hidden rounded-full bg-secondary">
//       <div
//         className={cn(
//           "-z-50 h-3 -translate-x-[34%] transform overflow-hidden rounded-full bg-blue-500",
//           className
//         )}
//       ></div>
//     </div>
//   )
// );

// ProgressBar.displayName = "ProgressBar";

// export { ProgressBar };
